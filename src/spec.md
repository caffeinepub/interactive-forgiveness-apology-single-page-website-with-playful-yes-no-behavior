# Specification

## Summary
**Goal:** Publish the existing apology site under the custom domain slug "sorrykiki".

**Planned changes:**
- Configure deployment/publishing settings to use the custom domain slug "sorrykiki" (lowercase) and ensure it conforms to domain rules.
- Update any necessary routing/asset base path or hosting configuration required for the site to load correctly under the custom domain, without changing UI/UX.

**User-visible outcome:** The apology site is accessible when published under the custom domain "sorrykiki" and functions the same as before.
