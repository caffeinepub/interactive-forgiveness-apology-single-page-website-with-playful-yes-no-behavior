{
  "kind": "build_request",
  "title": "Publish the apology site under the custom domain \"sorrykiki\"",
  "priority": "normal",
  "requirements": [],
  "constraints": [
    "Do not introduce any new backend canisters or services; keep the existing single-actor Motoko backend architecture unchanged.",
    "Do not edit any files under frontend/immutablePaths (frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useInternetIdentity.tsx, frontend/src/hooks/useActor.ts, frontend/src/main.tsx, frontend/src/components/ui).",
    "Use the custom domain slug exactly as \"sorrykiki\" (lowercase), and ensure it conforms to domain rules (5–50 characters, letters/numbers/hyphens only).",
    "Use English for any user-facing text in any new or modified UI (no new UI copy is requested for this publish-only step)."
  ],
  "nonGoals": [
    "Changing the app’s UI/UX, text content, animations, or styling beyond what is necessary to complete the publish step.",
    "Adding new features unrelated to publishing (e.g., new pages, auth flows, storage)."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}